<?xml version="1.0" encoding="utf-8"?>
<!--
  canPerformGestures      -> enables dispatchGesture() for coordinate taps/swipes.
  canRetrieveWindowContent-> enables reading the UI tree.
  flagRetrieveInteractiveWindows is deliberately NOT set: we only ever read the
  active window, which is a smaller privacy surface and enough for the actions
  the companion supports.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault|flagReportViewIds"
    android:canPerformGestures="true"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100" />
